/**
 * Configuração do banco para testes.
 * Usa as credenciais do sample por padrão; edite este arquivo para apontar ao seu MySQL local.
 * Este arquivo pode ser sobrescrito em CI (ex.: setupGHActions.js) ou ignorado no .gitignore
 * se você preferir não versioná-lo.
 */
export { config } from './testConfig.sample';
